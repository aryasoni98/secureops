apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openclaw-state
  labels:
    app.kubernetes.io/name: secureops
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
