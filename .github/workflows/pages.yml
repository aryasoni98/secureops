# GitHub Pages: landing page (docs/index.html - static bundle, no build step) at /,
# mkdocs-material docs at /docs/.
name: Pages

on:
  push:
    branches: [master]
    paths:
      - "docs/index.html"
      - "docs/**"
      - "mkdocs.yml"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# One deploy at a time; don't cancel a production deploy mid-flight.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Build docs (mkdocs-material)
        run: |
          pip install mkdocs-material
          mkdocs build --strict --site-dir _docs_build

      - name: Assemble site
        run: |
          mkdir -p _site
          cp docs/index.html _site/index.html
          mkdir -p _site/docs
          cp -r _docs_build/* _site/docs/
          # Bypass Jekyll processing on Pages.
          touch _site/.nojekyll

      - uses: actions/upload-pages-artifact@v4
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
